name: 'test_awspricelist'
version: '1.0.0'
config-version: 2

profile: 'snowflake-trial-aws-price-list'

dbt-cloud:
  project-id: 511729

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  +materialized: table
  +transient:    false
